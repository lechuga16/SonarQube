# --- SonarQube Web ---
# Puerto donde escucha SonarQube en el host (por defecto 9000)
SONARQUBE_PORT=9000

# --- DB (PostgreSQL) ---
POSTGRES_USER=sonar
POSTGRES_PASSWORD=sonar
POSTGRES_DB=sonar

# --- JDBC SonarQube ---
# URL de conexión JDBC al servicio de Postgres dentro del compose
SONAR_JDBC_URL=jdbc:postgresql://db:5432/${POSTGRES_DB}
SONAR_JDBC_USERNAME=${POSTGRES_USER}
SONAR_JDBC_PASSWORD=${POSTGRES_PASSWORD}

# --- Token de SonarQube (généralo en http://localhost:9000 -> My Account -> Security)
# No incluyas tokens reales en este archivo.
SONAR_TOKEN=

# --- Montajes disponibles para el contenedor del scanner ---
# Decláralos en whitelist.json -> mounts y comparte cada ruta en Docker Desktop (Settings > Resources > File Sharing)
# Ejemplo (ver whitelist.example.json):
#   "mounts": [
#     { "host": "C:/Repos", "container": "/work/src1" }
#   ]
